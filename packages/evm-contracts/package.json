{
  "name": "evm-contracts",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": {
      "default": "./index.ts"
    }
  },
  "scripts": {
    "lint": "solhint -w 0 `find script src test -name '*.sol' ! -name '.*'`",
    "build": "forge build",
    "dev": "npm run test-tdd -- --watch",
    "test-tdd": "FOUNDRY_PROFILE=tdd forge test",
    "test-ci": "FOUNDRY_PROFILE=ci forge test -vv",
    "test-coverage": "FOUNDRY_PROFILE=coverage forge coverage",
    "generate-types": "wagmi generate"
  },
  "devDependencies": {
    "@wagmi/cli": "^1.5.2",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
